import Testing
import Foundation
@testable import OnYourMarks

/// Anchor class used by tests to locate the test bundle via Bundle(for:).
final class _OnYourMarkedTestsAnchor: NSObject {}

@Test func appLaunches() {
    // Placeholder test — will be expanded in later tasks
}
